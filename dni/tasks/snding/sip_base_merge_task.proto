syntax = "proto3";

package dni;

message SndSIPBaseMergeTaskOptions {
    message ThreshLabels {
        double ratioMin = 1;
        double ratioMax = 2;
        repeated string label = 3;
    }
    ThreshLabels num_stat = 1;
    ThreshLabels proto_stat = 2;
}

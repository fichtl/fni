package(default_visibility = ["//visibility:public"])

licenses(["notice"])

cc_library(
    name = "onnxruntime",
    srcs = glob(["lib/*.so"]),
    hdrs = glob(["include/*.h"]),
    includes = ["include"],
)

package(default_visibility = ["//visibility:public"])

licenses(["notice"])

cc_library(
    name = "onnxruntime",
    srcs = glob(["lib/*.dylib"]),
    hdrs = glob(["include/*.h"]),
    includes = ["include"],
)
